# Terraform Infrastructure

This directory contains infrastructure-as-code documentation for the project.

The Terraform configuration is intended for local/development infrastructure and is not a production cloud deployment.

Production infrastructure should use managed PostgreSQL/Redis, encrypted remote Terraform state and a dedicated secret manager.
